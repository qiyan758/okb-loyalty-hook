# Top-level catch-all. Each subproject also has its own .gitignore.
node_modules/
.next/
dist/
build/
artifacts/
cache/
typechain/
typechain-types/
coverage/
coverage.json

# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key

# OS / editor
.DS_Store
.vscode/
.idea/
*.log
*.swp
